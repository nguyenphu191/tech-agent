services:
  web:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - NEXT_PUBLIC_SITE_URL=http://localhost:3000
      # Optional: Resend
      # - RESEND_API_KEY=
      # - CONTACT_FROM_EMAIL=
      # - CONTACT_TO_EMAIL=
